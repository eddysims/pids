{
  "name": "pids",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prerelease": "lerna publish --canary --contents dist"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "chalk": "^4.1.0"
  }
}
