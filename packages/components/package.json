{
  "name": "@pids/components",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc && node ../../scripts/moveFiles",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/react": "^17.0.2",
    "typescript": "^4.1.5"
  }
}
